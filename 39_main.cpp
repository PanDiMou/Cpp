#include <iostream>

/*int main() {
	std::cout << "Enter a number: ";
	unsigned int number{ 0 };
	
	if (std::cin >> number) {
		if (number > 0 && number <= 7) {
			std::cout << "Night" << std::endl;
		} else if (number >= 8 && number < 12) {
			std::cout << "Morning" << std::endl;
		} else if (number == 12) {
			std::cout << "Noon" << std::endl;
		} else if (number >= 13 && number <= 18) {
			std::cout << "Afternoon" << std::endl;
		} else if (number >= 19 && number < 24) {
			std::cout << "Evening" << std::endl;
		} else if (number == 0 || number == 24) {
			std::cout << "Midnight" << std::endl;
		}
		else {
			std::cerr << "Number must be between 0 and 24." << std::endl;
		}
	}
	else {
		std::cerr << "Invalid input." << std::endl;
	}

	return 0;
}*/
