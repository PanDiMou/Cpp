#include <iostream>

/*int main() {
	int kg{ 0 };
	std::cout << "Enter weight in kilograms: ";
	std::cin >> kg;

	if (kg <= 5) {
		std::cout << "Use the 5 kg washing machine" << std::endl;
	} else if (kg <= 10) {
		std::cout << "Use the 10 kg washing machine" << std::endl;
	} else {
		unsigned int numberOfMachines{ 0 };
		while (kg > 5) {
			kg -= 10;
			++numberOfMachines;
		}
		std::cout << "Use " << numberOfMachines << ", 10 kg washing machines" << std::endl;
		if (kg >= 0) {
			std::cout << "Use one 5 kg washing machine for the remaining " << kg << " kg" << std::endl;
		}
	}
	return 0;
}*/
